tap "cameroncooke/axe"
# UI automation. ios-mcp requires >= 1.8.0 for current simulator runtimes;
# `brew bundle` installs latest, existing installs need `brew upgrade`.
brew "cameroncooke/axe/axe"
brew "swiftlint"
